# Core libraries
pandas==2.1.4
numpy>=1.26.0,<1.28.0
python-dotenv==1.0.0

# API integrations
alpaca-py==0.21.0          # For paper/live trading
anthropic>=0.75.0          # For Claude API (0.75.0+ fixes httpx compatibility)
openai>=1.50.0             # For OpenAI API (1.50.0+ fixes httpx compatibility)
groq>=1.0.0                # For Groq API (1.0.0+ fixes httpx compatibility)

# Data and analysis
yfinance==0.2.36           # For market data
# ta-lib==0.4.28             # Optional, commented out due to compilation failure
pandas-ta>=0.3.14b0  # Technical analysis indicators (version depends on Python: >=0.4.67b0 for 3.12+, <0.4.67b0 for <3.12)
scipy>=1.11.4,<1.12.0      # Loosened if needed
statsmodels>=0.14.1,<0.15.0 # Loosened if needed

# Database
psycopg2-binary==2.9.9     # PostgreSQL (if using)
sqlalchemy==2.0.25         # ORM

# Utilities
requests==2.31.0
aiohttp==3.9.1             # Async HTTP
python-dateutil==2.8.2
pytz==2023.3
pydantic==2.5.0            # Schema validation for LLM responses

# Testing
pytest==7.4.4
pytest-asyncio==0.23.3
pytest-cov==4.1.0
pytest-mock==3.12.0
requests-mock==1.11.0     # For API mocking in tests (1.11.1 doesn't exist)

# Monitoring
prometheus-client==0.19.0  # Metrics (optional)
streamlit==1.31.0          # Dashboard UI
plotly==5.18.0             # Interactive charts for dashboard
